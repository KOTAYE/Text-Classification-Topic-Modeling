# CPU-only torch: no GPU is needed for inference, and the CUDA build is ~10x larger
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.13.0+cpu
transformers==5.14.1
httpx==0.28.1
fastapi==0.141.1
uvicorn[standard]==0.52.2
